To be done. For now, have a look at <project_dir>/scripts

specify module in .vscode/launch.json where name is Debug SimRun. A working project is "args": ["QD_DDFS5", "Config.yaml"],

We are going to work on TST_ACCUM3
